{
  "terminal.integrated.env.windows": {
    "PYTHONPATH": "${workspaceFolder};${env:PYTHONPATH}"
  },
  "python.terminal.executeInFileDir": false,

  "python.analysis.extraPaths": [
    "${workspaceFolder}/methods/MiniROAD" // currently used submodule
  ]
}